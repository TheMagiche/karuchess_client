// import config from "config";
// import { handleResponse, requestOptions } from "../_helpers";

// export const userService = {
//   getAll,
//   getById
// };

// function getAll() {
//   return fetch(`/api/member`, requestOptions.get()).then(handleResponse);
// }

// function getById(id) {
//   return fetch(`/api/member/${id}`, requestOptions.get()).then(handleResponse);
// }
